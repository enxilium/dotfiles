# --- OS Metadata (Mac/Linux/Windows) ---
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Desktop.ini

# --- Neovim / Vim (Swap & State) ---
# Temporary swap files (prevents crashes from being committed)
*.swp
*.swo
*~

# Persistent state/history (you don't want your command history in Git)
.netrwhist
Session.vim
shada/
state/

# --- LazyVim Specific ---
# Ignore the backup lockfile, but KEEP the real lazy-lock.json
lazy-lock.json.bak

# --- Secrets & Local Overrides ---
# Any file ending in .local is standard convention for machine-specific configs
*.local
*.local.*

# Specifically for your LazyVim setup
lua/config/local.lua

# --- Tags ---
# Generated by ctags/GTags for code navigation
tags
TAGS
GTAGS
GRTAGS
