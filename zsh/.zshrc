# UV setup
export PATH="$HOME/.local/uv/bin:$PATH"
# Source uv env for shell integration (auto Python/venv detection)
source "$HOME/.local/uv/bin/env"
ZSH="$HOME/.oh-my-zsh"
ZSH_THEME="robbyrussell"   # or your theme
plugins=(git)              # you can add other plugins if needed
source $ZSH/oh-my-zsh.sh

